[{{ rescue.board_index }}{% if show_id %}@{{ rescue.api_id }}{% endif %}] {{ rescue.client }}'s case ({{ rescue.platform.colorize() }}) in {% if rescue.system %}
    {{- rescue.system }} {% else %} an unspecified system {% endif %} {% if not rescue.active %} ({{ color("Inactive", Colors.GREY) }}) {% endif %}
    {% if (rescue.rats or rescue.unidentified_rats) and show_unident or (rescue.rats and not show_unident)  -%}
        Assigned Rats: {{rescue.rats.values() | map(attribute="name")| join(', ')}}
    {% if show_unident and rescue.unidentified_rats -%}

        Unidentified Rats: {{rescue.unidentified_rats.values() | map(attribute="name")| join(', ')}}
    {%- endif %}
    {%- else -%}
        No assigned Rats.
    {%- endif -%}